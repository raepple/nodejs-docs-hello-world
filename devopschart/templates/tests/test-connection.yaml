apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "devopschart.fullname" . }}-test-connection"
  labels:
    {{- include "devopschart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "devopschart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
